# Kea 规约与计划过程记录

## 1. 文档性质与证据边界

本文件记录 Kea 的设计、计划和课程补交过程。项目早期没有把完整聊天记录导出
到仓库，因此以下内容分为两类：

1. **可核验事实**：有 dated spec、plan、测试、验证记录或 commit hash 支撑。
2. **重建摘要**：根据相邻版本文档与 commit 差异概括当时问题和决策，不冒充
   逐字对话。

没有保存的 prompt、不同类型智能体冷启动对话和人工感受不能从 Git 历史反推，
对应缺口在第 7 节单独列出，需由本人补充真实材料。

## 2. Brainstorming 的总体演进

Kea 并非从一次完整 spec 直接实现，而是通过多轮“设计文档 → 评审修订 →
细粒度计划 → TDD 实现”逐步收敛。仓库中的主要证据是：

- `docs/superpowers/specs/`：按日期保存的设计。
- `docs/superpowers/plans/`：对应的实施计划，包含 RED/GREEN 命令和 commit
  建议。
- `docs/verification.md`：最终本机验证记录。
- Git 历史：设计、计划、实现、修复和文档各自独立提交。

最有价值的 brainstorming 问题并不是“还可以增加什么功能”，而是：

- Harness 与宿主编码智能体的边界在哪里？
- Hook、Event、工具执行和 UI 展示分别由哪一层拥有？
- 一个机制移除真实 LLM 后是否仍能确定性验证？
- Project、Session、cwd 和 memory 的生命周期是否一致？
- 课程分发与安全要求怎样落到用户真正能运行的 CLI 上？

## 3. 关键迭代一：从大而全的 Hook 设计收缩为分层契约

**时间：** 2026-08-13
**证据：**

- `docs/superpowers/specs/2026-08-13-hook-harness-tool-ui-design.md`
- commits `35f511a`、`d59b4f4`、`02b6362`

### 重建的对话节选（非逐字记录）

> 智能体问题：Hook 是控制行为，还是通知已经发生的事实？如果两者共用一个
> 抽象，错误传播和 UI 订阅如何保持一致？
>
> 人工决策：区分控制路径与观察路径；危险操作必须能阻止执行，展示监听器失败
> 不能改变运行结果。同时要求每次工具调用即使被拒绝也形成完整 Tool Result。

### 修正

初稿把 Hook、Harness 事件和工具 UI 放在一个较宽的设计中。评审后通过
`d59b4f4` 修订边界，再由 `02b6362` 拆成可执行计划。人工保留了“机制发生在
代码层”的建议，但拒绝让通用 Harness 直接拥有具体终端展示。

### 影响

这一轮形成后续架构的核心分工：Agent 执行循环、Harness 管 Session、Coding
Agent 组装领域能力、UI 只消费事实并渲染。

## 4. 关键迭代二：事件系统从多种语义收敛为 emit/intercept

**时间：** 2026-08-14
**证据：**

- `docs/superpowers/specs/2026-08-14-unified-event-system-design.md`
- `docs/superpowers/specs/2026-08-14-events-hardening-readme-design.md`
- commits `f77cbf7`、`7b96b7a`、`9fd832c`、`580aea7`、
  `43001e8`

### 重建的对话节选（非逐字记录）

> 智能体问题：ask、transform、事实通知和执行拦截是否真的需要四套运行时
> 原语？取消和 listener 错误由谁负责？
>
> 人工决策：用 `emit` 表示已发生事实，用 `intercept` 包装尚未提交的行为。
> 事实 listener 的异常被隔离；控制 listener 的异常穿透给行为所有者；取消信号
> 在每个异步边界检查。

### 修正

最初 unified-event 设计仍包含 `ask` 与 `transform`。实现和 hardening 后发现
它们与控制链语义重复，最终在 `43001e8` 收敛为 `emit/intercept`。这次修改
不是简单改名，而是删掉容易产生多重所有权的接口。

### 影响

工具执行前后、用户 prompt、context 和 stopping 都使用同一控制模型；Session
持久化发生在事实发布之前，UI 因而只能观察已决定状态。

## 5. 关键迭代三：从“库”转为可提交的 CLI 产品

**时间：** 2026-08-14
**证据：**

- `docs/superpowers/specs/2026-08-14-course-release-design.md`
- 四份 `2026-08-14-course-release-*.md` 实施计划
- commits `4a70a2c`、`b8d390e`、`1a499b3` 至 `5de14c5`

### 重建的对话节选（非逐字记录）

> 智能体问题：有限时间应优先做 Web 部署，还是完成凭据边界、机制演示、终端
> 体验和宿主平台可执行构建？
>
> 人工决策：保持 CLI 与本地优先；把时间投入可测试的治理、反馈、记忆、配置、
> mock 演示和单文件构建，不在这一轮加入 WebUI、MCP、向量记忆和自动发布。

### AI 建议与人工取舍

**采纳：**

- 使用用户级 `config.json` 与独立 `auth.json`，阻止项目配置携带 secret。
- 增加 turn/tool-call 上限、验证工具、显式项目记忆和 mock 机制演示。
- 采用 compact terminal cards，而不是重写为全屏 TUI。
- 使用 Node.js SEA 构建宿主平台可执行程序。

**推翻或缩减：**

- 没有加入完整命令框架、通用策略语言、向量数据库、MCP 和自动更新。
- 没有为“看起来像产品”而引入服务端或 WebUI。
- 没有把 UI listener 错误升级为 agent run 错误。

### 影响

课程 release 被拆成配置/CLI、机制、UI、打包文档四个计划；每个功能都先在计划
中写明失败测试，再形成独立实现 commit。

## 6. 关键迭代四：根据助教检查补齐根目录交付物

**时间：** 2026-08-16
**证据：** 本轮用户确认、根目录新增文件及
`docs/superpowers/specs/2026-08-16-course-deliverables-compliance-design.md`。

### 已保存的本轮决策

> 用户决定：PR 流程可以不需要，WebUI 也可以不需要，因为项目是个人、本地
> 开发；其余缺失项直接增加。REFLECTION 只新建文件，正文之后由本人完成。

据此选择同时增加 GitHub Actions 与 `.gitlab-ci.yml`：前者匹配实际 GitHub
仓库和助教检查，后者匹配课程文本的字面要求。没有为了凑项伪造 PR，也没有添加
空壳部署链接。

## 6.1 关键迭代五：课程发布后的架构收敛与标题修正

**时间：** 2026-08-18 — 2026-08-19
**证据：** commits `fdf5890` 至 `ed43a12`（详见 `AGENT_LOG.md`）。

### 重建的对话节选（非逐字记录）

> 智能体问题：早期为"高分项"引入的 verify 工具、显式项目记忆与宿主平台 SEA
> 单文件构建，是否真的让交付更稳？还是扩大了需要保证正确性的表面？
>
> 人工决策：删除 `verify`/`remember`/`search_memory` 与 SEA 单文件构建，分发改为
> `npm install -g .` 全局安装；移除 `init` 命令、改为首次运行自动创建配置模板；
> 统一 `agent`/`harness` 边界并把应用启动合入 `coding-agent`。

### 修正

这一轮把 course-release 的功能面收敛回真实、可完整测试的核心：6 个内置工具、
分层 JSON 配置、npm 全局分发。`ed43a12` 进一步修正 session title 的持久化方式，
把它从追加记录改为 header 字段并原地重写，避免编辑器实时打开文件时原子替换失败。

### 影响

- 分发与 CI 从 SEA 单文件改为 npm 全局安装（`bin` 字段），README 与两套 CI 配置
  一并对齐。
- 标题生成失败时回退到首条非空 prompt 文本，保证标题不长期停留在默认值。
- 恢复 `append` 对非法记录的内存校验（回归修复），`npm test` 331/333 通过。

## 7. 陌生智能体冷启动验证

### 当前证据状态

仓库中未发现能够证明“不同类型智能体、全新 session、只提供 SPEC + PLAN”
的完整对话或执行记录。因此本节不能诚实地填写为已完成。

### 本人需要完成并粘贴的真实记录

1. 选择与主开发智能体不同的工具，新建无 memory 的 session。
2. 只提供 `SPEC.md` 和 `PLAN.md`，要求它选择 1-2 个 task。
3. 明确要求遇到不确定即暂停提问，不接受额外口头解释。
4. 保存以下原始证据：
   - 智能体类型、日期和所选 task；
   - 第一次暂停时的原问题；
   - 与原意不同的解读；
   - 产出与预期差异；
   - 修改前后的 SPEC/PLAN 关键 diff。

建议按下面格式追加，方括号内容必须替换为真实记录：

```text
冷启动智能体：
日期：
选取 task：
首次提问：
暴露的规约缺陷：
修订前：
修订后：
验证结果：
```

## 8. 对 brainstorming 技能的阶段性评价

### 做得好的地方

- 强迫先定义所有权、错误和取消边界，再进入实现。
- 将模糊的“安全”“记忆”“反馈”改写成脱离真实模型也能测试的机制。
- 通过设计评审暴露 Hook/Event 重叠、Project/Session 生命周期和 UI 反向依赖。

### 不足

- 设计文档和计划数量很多，但早期没有同步维护一个根目录总览。
- 对话证据没有自动沉淀，导致事后只能从 artifact 和 commit 重建部分过程。
- 细粒度计划的 checkbox 未随 commit 回填，完成状态需要另外通过 Git 核验。
- 流程容易关注实现正确性，却遗漏助教只扫描根目录标准文件名这一交付现实。

## 9. 后续动作

- 本人补充第 7 节真实冷启动证据（仍待完成）。
- 提交并 push 当前交付物，确认最后一次 CI 运行为绿色并附上链接。
- 本轮课程材料与日志位于同一提交；提交 hash 通过 `git log` 查询。
