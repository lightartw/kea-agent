# Kea 项目反思报告

> 本文内容基于本人真实开发经历与观点整理。写作过程中使用 AI 协助组织结构和润色文字，核心判断、案例取舍与最终审阅由本人完成。

## 1. 项目与我的职责

Kea 是一个运行在终端中的 Coding Agent Harness。语言模型负责给出下一步动作，Harness 负责组织上下文、调用模型、执行工具、保存会话、拦截危险命令，并把测试结果反馈给模型。我的工作覆盖需求选择、模块设计、任务拆分、智能体协作、代码评审和最终验收。这个项目让我最深的体会集中在两件事上：怎样设计边界清楚的模块，以及怎样给 Coding Agent 提供足够准确的上下文。

## 2. 从核心概念开始设计模块

我现在设计模块时，会先写清它解决的问题、核心概念及概念之间的关系，然后才设计类和接口。Events 模块是最典型的例子。它的核心概念只有 `event`、`on`、`emit`、`intercept` 和 `listener`：event 表示有名称的运行事件，on 注册 listener，emit 发布已经发生的事实，intercept 让 listener 在行为提交前参与控制。读者理解这几个概念后，就能预测事件注册、事实通知和危险动作拦截的基本过程。

确定核心概念也能限制抽象数量。新类、接口、管理器只有在拥有独立职责、状态或约束时才值得存在。现有概念能够表达需求时，我不会增加近义实体。Events 提供扩展点，但它只负责事件分发和行为拦截；完整插件系统还需要发现、加载、权限、版本和生命周期管理，因此不能因为存在 listener 就把模块描述成插件框架。

术语统一同样重要。同一个概念在代码、类型、测试和文档中使用同一个名称，能够减少智能体自行补全含义的空间。类型也应由行为所属模块定义，例如 listener 类型由 Events 模块根据事件契约生成，外部模块只声明自己需要交换的数据。高级泛型集中在基础模块，并且必须带来真实的类型安全收益；业务模块应保持直接可读。

设计接口前，我会画出数据的完整路径。Kea 中，一条用户输入经过 Harness 进入 Agent，Agent 调用模型并取得工具请求，工具执行结果写入 Session，随后作为下一轮上下文返回给模型。每一步都要明确输入、输出、所有权、顺序、状态变化、错误传播和取消方式。这样可以检查一条结果究竟由谁产生、在哪里持久化、何时允许 UI 观察，也能防止多个模块维护同一份状态。

这些原则最终指向一个选择：实现当前需求下最简单的正确方案。复杂设计需要先证明简单方案无法满足正确性或维护性，再获得引入成本的理由。我会优先参考成熟项目和官方接口的行业惯例，把精力留给真正属于 Kea 的治理、反馈和会话问题，不为尚未出现的插件或扩展提前搭建框架。

## 3. 面向第一次阅读者书写文档

模块设计清楚以后，仍要让其他人能够建立同样的理解。我的文档采用渐进式讲解：从模块承担的职责和最小概念开始，给出一条完整行为路径，建立整体模型后再对应到目录、类型和函数。理解当前设计所需的信息优先出现，只有影响正确性时才展开实现细节。

我也会主动检查上下文对齐。文档读者第一次接触项目，不知道设计期间讨论过什么，也没有智能体的会话记忆。只存在于讨论中的方案不会进入正式说明，所有结论都要能从当前代码、测试或需求中找到依据。每个概念完整解释一次，后文直接使用；重复总结和没有推动理解的铺垫应当删除。句子尽量直接、自然，让有限的阅读精力用于理解系统。

## 4. 给 Coding Agent 的 Prompt 与上下文

有效 Prompt 的关键在约束质量。派发任务时，我会提供目标、核心概念、模块负责与不负责的范围、数据流、不允许破坏的不变量、涉及文件和验证命令。复杂方案还要说明引入理由；如果简单方案已经足够，智能体不能因为更通用或更像框架就增加抽象。遇到歧义时应暂停提问，不能凭猜测继续扩大修改范围。

我不会把整个历史对话直接塞给实现模型。冗余上下文会让废弃讨论重新进入代码和文档，也会诱导模型回答已经失效的问题。SPEC 和 PLAN 应当成为独立、可执行的上下文，其中的术语、接口和验收条件必须自洽。Prompt 再限定当前 task 所需的文件和测试，模型便能在较小范围内稳定工作。

这个项目中，我采用了分层模型协作。Codex 负责 SPEC、PLAN 和关键架构设计，因为这些任务需要较强的全局推理和边界判断；DeepSeek V4 Flash 根据计划完成具体实施；完成后再由 Codex review，检查术语、模块职责、数据流和测试证据，并据此迭代。高能力模型的成本集中在设计和评审，速度更快的模型承担明确的执行工作。这个组合能够提高效率，但前提是计划足够精确。计划里的一个歧义会被执行模型快速放大，所以 task 要控制在一次会话能够完成和验证的范围内，每项都写明文件、预期失败测试、最小实现和完成条件。

## 5. TDD 与客观反馈

TDD 对 Harness 尤其重要，因为很多错误发生在顺序、异常和取消边界。实现 Events 时，我先用测试规定 listener 调用顺序、异常隔离、重复注册和 AbortSignal 的行为，再写满足这些约束的实现。失败测试迫使我回答具体问题：某个 listener 抛错后其他 listener 是否继续，取消发生在异步 listener 返回后是否仍能阻止行为提交。仅靠接口名称无法表达这些细节。

因此，TDD 在本项目中更像可执行规约。它会增加前期思考时间，却减少了智能体凭局部直觉补全行为的机会。verification-before-completion 也很有价值，它要求用完整测试和构建结果支持“已经完成”的判断。对于 AI 生成代码，这条纪律能有效避免把语气上的自信当作工程证据。

## 6. 凭据与分发带来的思考

凭据要求让我意识到“能够调用模型”和“能够安全交给别人使用”之间还有很长距离。Kea 把共享配置与用户级 `auth.json` 分开，项目配置拒绝 secret，初始化不会覆盖已有文件，错误信息也不能回显 key。这些措施降低了误提交风险，但明文 auth 文件仍依赖本机账户和文件权限，无法达到系统钥匙串的保护强度。

分发同样迫使我限定承诺。Node SEA 可以生成宿主平台可执行文件，Windows 上的实际构建和 smoke test 只能证明 Windows 路径成立。Linux、macOS、代码签名和系统拦截都需要各自验证。README 中的安装命令、凭据配置和已知限制属于交付物的一部分，不能用“源码能够运行”代替。

## 7. 对 Superpowers 的评价

对我帮助最大的技能是 brainstorming、writing-plans、test-driven-development 和 verification-before-completion。它们分别约束需求澄清、任务粒度、行为证据和完成声明。对于个人本地项目，worktree、PR 和多阶段 review 的全部形式未必都能产生同等收益；我更关注设计与实现之间是否存在独立检查，以及每次修改能否被测试客观验证。

Superpowers 也容易产生大量文档和流程痕迹。如果缺少人工控制，计划会重复描述同一件事，智能体也可能为了满足形式继续增加实体。这个项目内部保存了许多设计和实施记录，却一度缺少标准根目录入口，说明过程完整和交付可发现性是两个问题。方法论只能提供纪律，最终仍需要人判断哪些抽象必要、哪些文字值得读、哪些证据真正支持结论。

如果重新开始，我会更早建立根目录 SPEC、PLAN 和过程索引，让详细文档围绕单一入口组织；继续采用高能力模型设计、快速模型实施、高能力模型复核的组合；每轮 review 都检查核心概念、术语、数据流、模块边界、测试和读者上下文。对我而言，使用 Coding Agent 的关键能力已经从“写出更多代码”转向“给出足够清晰的约束，并对最终系统负责”。
